import 'package:flutter/material.dart';

const Color kBackground = Color(0xFF0D0D14);
const Color kSurface = Color(0xFF1A1A2E);
const Color kPrimary = Color(0xFF7C3AED);
const Color kSecondary = Color(0xFF3B82F6);

const Color kTextPrimary = Colors.white;
const Color kTextSecondary = Color(0xFFB0B0C3);

const Color kBorder = Color(0xFF2D2D44);
const Color kError = Color(0xFFEF4444);
const Color kSuccess = Color(0xFF22C55E);

const BorderRadius kCardRadius = BorderRadius.all(Radius.circular(20));

const LinearGradient kAccentGradient = LinearGradient(
  colors: [kPrimary, kSecondary],
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
);

